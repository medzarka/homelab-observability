services:

  # ========================================================
  # INITIALIZATION SERVICE (Automated Host Storage Setup)
  # ========================================================
  init-volumes:
    image: alpine:latest
    container_name: observability_init_volumes
    user: "0:0"
    restart: "no"
    network_mode: none
    volumes:
      - ${DATA_DIR:-/home/mgrsys/DATA}/observability/data:/data
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        echo ">>> [Init] Initializing Observability persistent storage directories..."
        mkdir -p /data/beszel /data/uptime-kuma /data/homepage/logs
        chmod -R 775 /data
        echo ">>> [Init] Observability storage initialized successfully!"

  # ========================================================
  # HOMEPAGE (Unified Homelab Portal & Service Cockpit)
  # ========================================================
  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage
    restart: unless-stopped
    volumes:
      - ./config:/app/config
      - ${DATA_DIR:-/home/mgrsys/DATA}/observability/data/homepage/logs:/app/config/logs
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - TZ=${TZ:-UTC}
      - HOMEPAGE_ALLOWED_HOSTS=${HOMEPAGE_ALLOWED_HOSTS:-homelab.example.com,hub.example.com,127.0.0.1,localhost}
    networks:
      - shared_net
      - homelab_swarm_net
    depends_on:
      init-volumes:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ========================================================
  # BESZEL HUB (Telemetry, Bandwidth & Resource Metrics)
  # ========================================================
  beszel:
    image: henrygd/beszel:latest
    container_name: beszel
    restart: unless-stopped
    ports:
      - "8090:8090"
    volumes:
      - ${DATA_DIR:-/home/mgrsys/DATA}/observability/data/beszel:/beszel_data
    environment:
      - PORT=8090
    networks:
      - shared_net
      - homelab_swarm_net
    depends_on:
      init-volumes:
        condition: service_completed_successfully
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ========================================================
  # BESZEL AGENT (Collector on VM1: zap-vps)
  # ========================================================
  beszel-agent:
    image: henrygd/beszel-agent:latest
    container_name: beszel_agent
    restart: unless-stopped
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /:/extra-filesystems/root:ro
    environment:
      - PORT=45876
      - KEY=${BESZEL_PUBLIC_KEY:-}
    depends_on:
      - beszel
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ========================================================
  # DOZZLE HUB (Real-Time Container Log Viewer)
  # ========================================================
  dozzle:
    image: amir20/dozzle:latest
    container_name: dozzle
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - DOZZLE_REMOTE_AGENT=${DOZZLE_REMOTE_AGENT:-}
      - DOZZLE_LEVEL=info
    networks:
      - shared_net
      - homelab_swarm_net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ========================================================
  # UPTIME KUMA (Multi-Node Health & Outage Alerting)
  # ========================================================
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: uptime_kuma
    restart: unless-stopped
    volumes:
      - ${DATA_DIR:-/home/mgrsys/DATA}/observability/data/uptime-kuma:/app/data
    networks:
      - shared_net
      - homelab_swarm_net
    depends_on:
      init-volumes:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "extra/healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  homelab_swarm_net:
    name: homelab_swarm_net
    external: true
  shared_net:
    name: ${SHARED_NETWORK:-shared_net}
    external: true
